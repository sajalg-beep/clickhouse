apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: clickhouse

resources:
  - service-account.yaml
  - configmap.yaml
  - cronjob.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app.kubernetes.io/name: clickhouse-backup
  app.kubernetes.io/component: backup
  app.kubernetes.io/part-of: clickhouse-cluster

# Override backup bucket name
# configMapGenerator:
#   - name: clickhouse-backup-config
#     behavior: merge
#     files:
#       - config.yml
